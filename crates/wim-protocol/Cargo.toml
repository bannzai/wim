[package]
name = "wim-protocol"
version = "0.0.0"
description = "Wire protocol shared by the wim daemon and its clients. Pure crate with no IO, buildable for native and wasm32."
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
